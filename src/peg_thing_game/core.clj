(ns peg-thing-game.core
  require [clojure.set] :as set
  (:gen-class)

(declare successful-move prompt-move game-over query-rows)
  
(defn foo
  "I don't do a whole lot."
  [x]
  (println x "Hello, World!"))

